# config file for the hbz pdfaService based on 
# pdfaPilot CLI (Callas)
tempDir = temp
resultDir = result

# Server config
workDir = /srv/tomcat/webapps

# Client config
host = pdfa.hbz-nrw.de
port = 443
path = soap
 